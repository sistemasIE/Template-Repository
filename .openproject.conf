# Configuracion OpenProject para este repositorio
# Cambiar PROJECT_ID segun el proyecto en OpenProject.
# Para consultar IDs:
#   docker exec -it openproject-db-1 psql -U postgres -d openproject
#   SELECT id, name FROM projects;

PROJECT_ID=0
TYPE_ID=10
